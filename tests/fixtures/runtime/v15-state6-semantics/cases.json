{
  "kind": "vac_runtime_state6_semantics_fixture",
  "policy_probes": [
    {
      "id": "default_deny_explicit_filesystem_read_allow",
      "default_decision": "deny",
      "rules": [
        {"id": "vac.policy.allow_read_workspace", "action": "filesystem_read", "path": "workspace", "decision": "allow"}
      ],
      "request": {"action": "filesystem_read", "path": "vac-rs/core/src/lib.rs"},
      "expected": "allow"
    },
    {
      "id": "default_deny_explicit_execute_process_approval",
      "default_decision": "deny",
      "rules": [
        {"id": "vac.policy.approval_for_process", "action": "execute_process", "path": "workspace", "decision": "approval_required"}
      ],
      "request": {"action": "execute_process", "path": "workspace"},
      "expected": "approval_required"
    },
    {
      "id": "explicit_deny_still_wins",
      "default_decision": "allow",
      "rules": [
        {"id": "vac.policy.allow_read_workspace", "action": "credential_read", "path": "workspace", "decision": "allow"},
        {"id": "vac.policy.hard.deny_credentials", "action": "credential_read", "path": "workspace", "decision": "deny"}
      ],
      "request": {"action": "credential_read", "path": "workspace"},
      "expected": "deny"
    },
    {
      "id": "no_match_applies_default_only_then",
      "default_decision": "deny",
      "rules": [],
      "request": {"action": "network_access", "path": "workspace"},
      "expected": "deny"
    }
  ],
  "approval_flow_cases": [
    "approval_required_persists_request_v2",
    "approval_required_reopens_waiting_for_tool_approval",
    "operator_accept_installs_single_retry_scoped_grant",
    "approved_retry_stamps_vac_bound_approval"
  ],
  "bootstrap_cases": [
    "missing_task_defaults_to_needs_discussion",
    "missing_spec_defaults_to_needs_discussion",
    "missing_todo_defaults_to_needs_discussion_unchecked_blocking_item",
    "default_closeout_evidence_invalid_and_blocking_reason_visible",
    "historical_bootstrap_terminal_artifacts_not_used_as_production_fallback"
  ]
}
