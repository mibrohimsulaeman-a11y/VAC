schema_version: 1
kind: test_fixture
id: fixture.evidence.ed25519-required
title: VAC-Init evidence signature envelope verifies canonical payload
status: ready
fixture:
  algorithm: ed25519
  payload: canonical_evidence_payload_without_self_hash
  canonical_rules:
    - sorted keys
    - UTF-8
    - LF newline
    - self_hash excluded
  required_errors:
    - UnsupportedAlgorithm
    - InvalidEncoding
    - VerificationFailed
validation:
  source: vac-rs/core/src/control_plane/vac_init_evidence_chain.rs
  symbols:
    - EvidenceSignatureEnvelope
    - evidence_signature_payload
    - verify_evidence_ed25519_signature
