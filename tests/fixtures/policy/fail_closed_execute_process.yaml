schema_version: 1
kind: policy
id: vac.policy.fixture.fail-closed
title: Fail closed fixture
default_decision: deny
rules:
- id: deny.execute
  match:
    action: execute_process
  decision: deny
  reason: fixture
