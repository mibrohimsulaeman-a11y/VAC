schema_version: 1
kind: policy
id: vac.policy.fixture.fail_closed
title: Fail Closed Fixture
default_decision: deny
rules:
  - id: fixture.allow.read
    match:
      action: filesystem_read
      path: project
    decision: allow
    reason: safe project read
