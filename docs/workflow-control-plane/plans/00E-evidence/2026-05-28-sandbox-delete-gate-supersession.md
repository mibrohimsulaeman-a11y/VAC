# 00E delete-gate supersession evidence — 2026-05-28 sandbox

## Decision

Plan 00E remains the historical old-runtime reachability gate, but its 2026-05-23 inverse-tree statements are no longer current default-path source-of-truth. Plan 33 owns active app-server Cargo delete/defer proof.

## Current state

- Default local product path is app-server-retired for DTO/runtime ownership.
- Physical `vac-app-server*` workspace crate deletion remains explicit operator-gated defer material.
- Docs must not claim the default TUI is still app-server-backed.

## Validation

Static docs sweep rejects stale active claims in non-evidence prose. The checked class includes old statements that say app-server is still the default runtime owner or that Plan 30G is unfinished. Those statements may be described here only as superseded concepts, not copied as active source-of-truth claims.
