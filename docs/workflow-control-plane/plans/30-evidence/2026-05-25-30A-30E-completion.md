# Plan 30 evidence — 30A–30E completion

Date: 2026-05-25

## Status summary

Plan 30 is **partially complete / in progress**. Slices 30A–30E are implemented and user-confirmed, but Plan 30 is not complete because 30F and 30G remain pending.

## Completed slices

- **30A — Prompt submit/read-only-first owner command bus**
  - Implemented 2026-05-24.
  - `RuntimeCommandBus` owns the read-only account/thread-list command shape and owner-backed start-turn command.
  - The TUI direct retained-manager path reads account state through the owner bus before prompt submit uses the same bus.

- **30B — Interrupt/steer/compact/shell controls**
  - Implemented 2026-05-25; user-confirmed.
  - Direct retained-manager paths for `turn_interrupt`, `turn_steer`, `thread_compact_start`, and `thread_shell_command` route through `RuntimeCommandBus`.

- **30C — Review + goals**
  - Implemented 2026-05-25; user-confirmed.
  - Direct local-runtime `review/start` and thread goal get/set/clear route through `RuntimeCommandBus` while preserving validation behavior.

- **30D — Realtime controls**
  - Implemented 2026-05-25; user-confirmed.
  - Direct local-runtime realtime start/audio/stop route through `RuntimeCommandBus`; the `RealtimeConversation` feature gate remains enforced before realtime core ops.

- **30E — Config/account/memory controls**
  - Implemented 2026-05-25; user-confirmed.
  - Direct TUI config reload, account logout, memory reset, and thread memory-mode set cross `RuntimeCommandBus` before touching retained local-runtime resources.

## Explicit caveats / blockers

- 30A–30E completion is recorded as automated/user-confirmed documentation evidence; this does not replace required operator proof for the TUI manual smoke gate.
- **30F is not complete**: real TTY operator smoke is still required, or the result must be honestly recorded as `BLOCKED-OPERATOR` in a headless/non-TTY environment.
- **30G is not complete**: skills/plugins surface and external-agent config still need migration to the local runtime owner or explicit blockers.
- **Plan 31 implementation remains blocked** until both 30F and 30G are complete.
