# Manifest Schemas

VAC schema contracts are implemented in the root control-plane crate and exercised by `vac doctor registry .`.

Use `.vac/capabilities/`, `.vac/policies/`, `.vac/surfaces/`, and `.vac/workflows/` as the source of truth for current manifest shape.

