# VAC v1.9 Traceability Matrix — P0 Spec-Conformance Closure

Status: working P0 closure artifact. This matrix maps v1.9 normative requirements and fixture groups to implementation, doctor gates, executable fixtures/tests, and current verification status. It does **not** claim full P0 acceptance by itself; it identifies which claims are TV/SV covered and which remain pending.

## Legend

| Status | Meaning |
| --- | --- |
| TV-Pass | Executed in real local validation during this slice or covered by Rust/Python test that was run. |
| SV-Pass | Static/source validation coverage exists and is executable, but runtime behavior may still need broader TV. |
| TV-Pending | Implementation or fixture exists only partially, or full runtime/CI gate was not executed in this slice. |
| Manual-Review | Spec requirement is UX/claim-language/operator-policy oriented and requires explicit review criteria. |

## Normative requirement traceability

| Req ID | Spec section | Requirement summary | Implementation module / artifact | Doctor gate | Fixture / test | Status | Gap / next closure |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R-003.3-surface-honesty | §3.3 | CLI/TUI/doctor/release output must not claim stronger guarantee than weakest derived trust. | `vac-doctor::doctor_release_trust_language`; `vac-rs/crates/surfaces/vac-cli/src/commands/control_plane.rs`; `.vac/vac.toml` L1 claim wording | `release`, `evidence`, `enforcement` | `vac-doctor` unit tests `release_doctor_*`; `scripts/check-vac-v19-fixture-coverage.py` | TV-Pass | Expand to golden CLI output snapshots for every doctor gate. |
| R-005.1-storage-classes | §5.1 | Authority manifests tracked; runtime/generated state not treated as source authority. | `.vac/vac.toml`; `.vac/migrations/runtime-db`; `.gitignore`; `scripts/check-v19-storage-classes.py`; `scripts/check-v19-package-hygiene.py` | `runtime-db`, package hygiene | `check-v19-storage-classes.py`, `check-v19-package-hygiene.py` | SV-Pass | Add package job to CI if not already wired. |
| R-005.2-manifest-hash | §5.2 | `manifest_set_hash` is deterministic JCS hash of compiled authority snapshot and excludes volatile metadata. | `vac-jcs`; `vac-registry-compiler`; `scripts/compile-vac-registry-sv.py` | `compiled`, `manifest-sync` | `vac-registry-compiler` tests; deterministic compile/index scripts | SV-Pass | Add two-pass cargo-level fixture over temp manifest set. |
| R-005.3-runtime-binding | §5.3 | Runtime records influencing future action carry manifest binding and git state; stale records cannot authorize current work. | `.vac/migrations/runtime-db/0001_runtime_journal.sql`; `vac-state::RuntimeManifestBinding`; `evaluate_runtime_event_append`; `evaluate_runtime_decision_authorization` | `runtime-db`, `manifest-sync` | `vac-state` unit tests: `runtime_state_tables_are_manifest_bound`, `runtime_event_append_requires_current_manifest_binding`, `stale_decision_cannot_authorize_current_action` | TV-Pass | Integrate with actual SQLite writer path when writer is implemented beyond projections. |
| R-005.4-manifest-sync | §5.4 | Manifest-sync classifies current/branch drift/stale/ghost/orphan; ghost/orphan quarantined. | `vac-state::classify_manifest_sync_record`; `vac-doctor::doctor_manifest_sync_records`; `vac-cli::doctor_manifest_sync` | `manifest-sync` | `vac-state` unit test `manifest_sync_classifies_ghost_and_orphan_state`; `vac-doctor` unit test `manifest_sync_doctor_blocks_ghost_stale_and_orphan_records`; `tests/fixtures/v19/manifest-sync/classification-cases.json`; coverage script tokens | TV-Pass | Live runtime DB scan pending until DB writer path exists; doctor reducer now blocks stale/ghost/orphan record projections. |
| R-006.1-stored-vs-derived-trust | §6.1 | Records store only trust claims/proof refs; derived trust is computed at read time, not stored as authority. | `vac-state::RuntimeTrustClaim`; `vac-state::derive_runtime_trust_at_read`; `vac-evidence::SignatureRecord::warning_label` | `evidence`, `release` | `vac-state` unit test `derived_trust_downgrades_unverified_attestation_at_read_time`; `vac-doctor` release trust tests | TV-Pass | Add doctor JSON output snapshot showing downgrade wording. |
| R-006.2-proof-downgrade | §6.2 | Custody above self-promoted must verify proof at read time; missing/invalid proof downgrades. | `derive_runtime_trust_at_read`; `doctor_release_trust_language` | `evidence`, `release` | `vac-state` and `vac-doctor` tests | TV-Pass | Add proof validator adapters for CI/broker/external anchors. |
| R-006.3-claim-language | §6.3 | Surface wording must match execution/custody axes and avoid proof-truth overclaims. | `RuntimeDerivedTrust.wording`; `doctor_release_trust_language`; `.vac/vac.toml` claim | `release`, `evidence` | `release_doctor_blocks_l1_local_overclaim_language`; `release_doctor_warns_for_honest_l1_local_claim` | TV-Pass | Golden-output coverage still pending. |
| R-007.2-runtime-db-schema | §7.2 | P0 runtime DB includes sessions/events/decisions and manifest-bound runtime tables. | `.vac/migrations/runtime-db/0001_runtime_journal.sql`; `vac-state::RUNTIME_DB_REQUIRED_TABLES` | `runtime-db` | `scripts/check-v19-runtime-db-schema.py`; `vac-state` schema tests | TV-Pass | Add actual migration runner fixture beyond source SQL execution. |
| R-007.2-unique-event-seq | §7.2/§27 | Duplicate `(session_id, seq)` must be rejected. | `runtime_events UNIQUE(session_id, seq)` migration | `runtime-db` | `scripts/check-vac-v19-fixture-coverage.py` executes migration in temp SQLite and verifies duplicate insert raises `IntegrityError` | TV-Pass | None for source schema; live runtime writer still pending. |
| R-007.3-writer-lease | §7.3 | Writer lease uses WAL, FK, busy timeout, `BEGIN IMMEDIATE`, heartbeat-counter recovery, and transaction-scoped seq allocation. | `vac-state::runtime_journal_write_plan`; SQL migration; `ACQUIRE_WRITER_LEASE_SQL`; `ALLOCATE_EVENT_SEQUENCE_SQL` | `runtime-db` | `vac-state` test `runtime_journal_write_plan_requires_begin_immediate_and_wal`; `check-v19-runtime-db-schema.py` | TV-Pass | Concurrency race fixture over actual DB writer remains pending. |
| R-007.4-runtime-db-migration | §7.4 | Runtime DB migrations are versioned under `.vac/migrations/runtime-db` and doctor reports schema/migration/binding support. | `.vac/migrations/runtime-db/0001_runtime_journal.{sql,yaml}`; `vac-cli::doctor_runtime_db` | `runtime-db` | `check-v19-runtime-db-schema.py`; coverage script | SV-Pass | Add multi-version migration harness when migration count >1. |
| R-008.1-authority-manifests | §8.1 | Only tracked authority manifests change runtime authority. | `.vac/capabilities`, `.vac/policies`, `.vac/workflows`, `.vac/surfaces`, `.vac/specs/confirmed`, `.vac/schemas`, `.vac/migrations` | `registry`, `compiled`, `policy`, `workflow` | `compile-vac-registry-sv.py`; `vac-registry-compiler` tests | SV-Pass | Add negative fixture for generated state attempting authority. |
| R-008.3-compiled-snapshot | §8.3 | Runtime consumes compiled strict JSON snapshots; dangling refs/duplicates/nondeterminism fail closed. | `vac-registry-compiler`; `.vac/cache/compiled`; `.vac/registry/compiled` | `compiled`, `registry` | `compile-vac-registry-sv.py`; registry compiler tests | SV-Pass | Full JCS two-pass fixture should be added to cargo tests. |
| R-009.2-decision-classifier | §9.2 | Decision class assigned deterministically; ambiguity escalates; sensitive floors apply. | `vac-policy`; runtime bound controller; `.vac/policies` | `policy`, `ownership` | Partial policy tests and traceability coverage | TV-Pending | Explicit deterministic decision-classifier fixture is still needed. |
| R-009.3-mechanical-floors | §9.3 | Hard-deny `.vac/db/**`/cache patch targets; authority paths impose authority mutation; sensitive paths impose team relevant. | `HARD_DENY_PATCH_PATHS`; `vac-agent-loop::BoundRuntimeController::pre_patch_gate`; `.vac/policies` | `policy`, `runtime-db`, `ownership` | `hard_deny_patch_blocks_vac_db_even_if_plan_claims_it`; `tests/fixtures/v19/patch/hard-deny-vac-db.json`; coverage script | TV-Pending | `.vac/db/**` hard-deny fixture closed; authority-mutation/team-relevant classifier floors still pending. |
| R-009.4-command-seam | §9.4 | Structured commands should not expose `.vac/db/**` as writable; unexpected subprocess DB mutation blocks completion and emits `runtime_db_touched_by_subprocess`. | `vac-state::evaluate_subprocess_runtime_db_mutation`; `vac-agent-loop::BoundRuntimeController::pre_command_gate` | `command`, `runtime-db`, `release` | `vac-state` test `subprocess_runtime_db_mutation_blocks_completion`; coverage script | TV-Pass | Wire hash snapshot check into actual validation command runner. |
| R-010-bounded-workflow | §10 | Normal session follows bounded workflow: semantic plan, gates, bounded patch, structured validation, closeout checks. | `vac-agent-loop`; `runtime_e2e`; TUI plan mode | `release`, `manifest-sync`, `readiness` | v1.5/v1.9 runtime e2e scripts; TUI lifecycle smoke | TV-Pending | DB-backed completion lock still partial. |
| R-011.1-policy-precedence | §11.1 | Most-restrictive-wins policy; explicit denies win. | `vac-policy::most_restrictive`; `PolicySnapshot::evaluate`; `vac-agent-loop::types` | `policy` | `vac-policy` tests; runtime e2e `most_restrictive_wins` | TV-Pass | Add manifest-level policy fixture in v1.9 coverage script later. |
| R-011.2-structured-commands | §11.2 | Structured commands must reject shell strings, pipes, redirection, wildcard expansion, unknown runner, and side effects not covered by policy. | `vac-agent-loop::bound_runtime::pre_command_gate`; `bound_tool::structured_command_from_value` | `command`, `policy` | `vac-agent-loop` tests; coverage script token checks | TV-Pass | Add command-runner integration fixture with real command JSON. |
| R-011.3-patch-gate | §11.3 | Patch valid only if plan scope, ownership, forbidden path, budget, and manifest binding pass. | `vac-agent-loop::bound_runtime`; `HARD_DENY_PATCH_PATHS`; fixtures under `tests/fixtures/runtime-agent-e2e` and `tests/fixtures/v19/patch` | `policy`, `ownership`, `manifest-sync` | `hard_deny_patch_blocks_vac_db_even_if_plan_claims_it`; `tests/fixtures/v19/patch/hard-deny-vac-db.json`; runtime e2e fixtures | TV-Pending | `.vac/db/**` hard-deny integration closed; full manifest-bound patch writer fixture still pending. |
| R-012-ownership | §12 | Source files should map to capability ownership; unowned/overclaimed hard violations block writes. | `.vac/capabilities`; registry compiler; `doctor_ownership` | `ownership` | registry/status checks | TV-Pending | Need explicit hidden/unowned/overclaimed fixture groups. |
| R-013-readiness | §13 | Assessment cannot raise readiness; explicit state reduction; aggregate inherits weakest trust. | `vac-readiness::compute_readiness`; `ReadinessTriplet::fail_closed`; compiler readiness summary | `readiness`, `release` | `vac-readiness` tests; `compile-vac-registry-sv.py`; coverage script | SV-Pass | Add `blocked/deprecated` v1.9 states to Rust enum/model. |
| R-014.2-index-determinism | §14.2 | Index records sorted by stable keys, raw span hash, no floats, low confidence represented honestly. | `generate-deterministic-index-sv.py`; `vac-index` | `index` | `check-checkpoint-integrity.py`; `vac-index` tests | SV-Pass | Full parser-backed product index remains pending. |
| R-014.3-span-normalization | §14.3 | Span raw hash and normalized fingerprint have distinct behavior; normalized fingerprint survives EOL/trailing-whitespace-only changes. | `vac-index::raw_span_sha256`; `vac-index::normalized_text_fingerprint`; deterministic index artifacts | `index` | `crlf_only_change_refreshes_raw_hash_but_keeps_normalized_fingerprint`; `tests/fixtures/v19/index/crlf-normalization.json` | TV-Pass | Full parser-backed product index remains pending, but CRLF raw/normalized fixture is closed. |
| R-014.4-missing-code | §14.4 | Absence may only be claimed with full coverage; otherwise downgrade to `not_found_in_index`/`coverage_insufficient`. | `vac-assessment::MissingCodeProof`; `evaluate_missing_code_proof`; assessment scripts | `assessment`, `index` | `missing_code_*` triad unit tests; `tests/fixtures/v19/missing-code/triad.json`; coverage script | TV-Pass | Product assessment generator still needs to emit these proof records end-to-end for every missing-code finding. |
| R-015-assessment-grounding | §15 | Semantic findings must cite deterministic evidence or bounded missing-code proof; baseline debt carries manifest binding and cannot authorize after drift. | `vac-assessment`; `.vac/assessment`; `check-assessment-freshness.py`; `vac-state` stale decision primitive | `assessment`, `manifest-sync` | `vac-assessment` tests; freshness script; `stale_decision_cannot_authorize_current_action` | TV-Pass | Baseline debt DB persistence fixture still pending. |
| R-016-completion-lock | §16 | Done only if plan/todos/validation/decisions/ownership/manifest-sync/spec-sync/governance/evidence terminal/current. | runtime e2e scripts; `vac-state` decision/manifest primitives | `release`, `manifest-sync`, `readiness`, `governance`, `evidence` | v1.5 runtime e2e fixture set | TV-Pending | DB-backed closeout/terminal-state fixture needed. |
| R-017-micro-slice | §17 | Micro-slice eligibility is classifier-driven and cannot be agent-self-declared. | runtime classifier/e2e scripts | `policy`, `governance` | v1.5 runtime e2e adoption/friction fixtures | TV-Pending | Add v1.9 micro-slice classifier fixture. |
| R-018-memory | §18 | Memory never relaxes policy/ownership/readiness/approval/command gates; secrets/raw CoT not persisted. | `vac-memory`; `vac-foundation::secret_manager`; runtime journal storage classes | `memory`, `policy`, `evidence` | `secret_redaction_regressions`; memory crate tests | TV-Pending | Add memory-does-not-authorize policy fixture. |
| R-019-evidence | §19 | P0 evidence is local cooperative integrity hint unless proof upgrades custody. | `vac-evidence`; `RuntimeTrustClaim`; `doctor_release_trust_language` | `evidence`, `release` | `vac-evidence` tests; `vac-doctor` release tests | TV-Pass | Add evidence doctor proof-material fixtures. |
| R-020-governance | §20 | Governance records raw events and advisory score; denominator/numerator reproducible; no release-grade enforcement overclaim in P0. | `vac-doctor::reduce_governance_window`; `GovernanceWindowReport`; `.vac/vac.toml` L1 claim wording | `governance`, `release` | `governance_zero_denominator_uses_max_one_and_passes`; `governance_reducer_matches_spec_denominator_breakdown`; governance JSON fixtures | TV-Pass | Wire reducer into `vac doctor governance` CLI output path. |
| R-023-cli-doctor-surfaces | §23 | Required P0 commands/gates exist: compile registry, doctor registry/manifest-sync/runtime-db/policy/ownership/index/readiness/governance, why. | `vac-cli::commands::control_plane`; CLI command parser | doctor gates | CLI tests + command parser tests | TV-Pending | `doctor governance`, `doctor readiness`, and `vac why` require explicit command-path verification. |
| R-024-doctor-taxonomy | §24 | Doctor gate blocks under specified invalid states; output displays read-time trust wording. | `vac-doctor`; `doctor_release_trust_language`; `tests/fixtures/v19/doctor-output` | all doctor gates | `doctor_trust_wording_golden_snapshots_match`; coverage script | TV-Pending | Trust wording golden snapshots closed; full gate-by-gate golden output snapshots still pending. |
| R-025-fixtures-traceability | §25 | P0 must include executable fixtures; every normative required statement should map to doctor/fixture/manual review; RC cannot claim P0 until required fixture groups pass. | This document; `scripts/check-vac-v19-fixture-coverage.py` | `fixture-coverage` | coverage script itself | TV-Pass | Keep matrix updated as gates move from pending to pass. |
| R-027-acceptance | §27 | P0 acceptable only when all acceptance criteria are met and release doctor does not overclaim L1 local records. | This matrix; `vac-state`; `vac-doctor`; scripts | `release`, `fixture-coverage` | coverage script acceptance checks | TV-Pending | Full P0 acceptance not claimed until all pending rows close and CI confirms. |

## Minimum fixture group coverage

| Fixture group | Required negative/edge coverage | Current implementation / fixture | Status | Gap / next closure |
| --- | --- | --- | --- | --- |
| FG-runtime-db | migrations, WAL, `BEGIN IMMEDIATE`, heartbeat-counter lease recovery, sequence allocation; duplicate `(session_id, seq)` rejected | `.vac/migrations/runtime-db/0001_runtime_journal.sql`; `vac-state` tests; `scripts/check-v19-runtime-db-schema.py`; coverage script executes temp SQLite duplicate insert | TV-Pass | Actual DB writer race fixture pending. |
| FG-manifest-sync | manifest hash, git HEAD binding, stale state, ghost state; stale decision cannot authorize current action | `classify_manifest_sync_record`; `evaluate_runtime_decision_authorization`; `doctor_manifest_sync_records`; `doctor_manifest_sync`; `tests/fixtures/v19/manifest-sync/classification-cases.json` | TV-Pass | Live runtime DB scan pending until writer path exists; projection reducer/quarantine semantics closed. |
| FG-trust-vector | stored claim vs read-time derived trust; downgrade on missing proof | `derive_runtime_trust_at_read`; `doctor_release_trust_language` | TV-Pass | Proof adapter fixtures pending. |
| FG-policy-patch | structured command, forbidden paths, hard-deny patch; `.vac/db/**` patch rejected | `vac-policy`; `vac-agent-loop::BoundRuntimeController`; `HARD_DENY_PATCH_PATHS`; `tests/fixtures/v19/patch/hard-deny-vac-db.json` | TV-Pass | Full manifest-bound patch writer fixture still pending. |
| FG-command-seam | Validation command mutates `.vac/db/**` unexpectedly and completion blocks | `evaluate_subprocess_runtime_db_mutation` | TV-Pass | Wire into actual command runner; currently primitive-level. |
| FG-governance | weighted numerator, denominator, max(1, ...), thresholds; zero-denominator no divide by zero | `vac-doctor::reduce_governance_window`; `tests/fixtures/v19/governance/*.json` | TV-Pass | Wire into CLI doctor governance command path. |
| FG-span-index | raw-byte hash, normalized fingerprint, EOL normalization, formatter-stable anchor | `vac-index::raw_span_sha256`; `vac-index::normalized_text_fingerprint`; `tests/fixtures/v19/index/crlf-normalization.json` | TV-Pass | Full parser-backed index still pending. |
| FG-missing-code | absent, not_found_in_index, coverage_insufficient; low-confidence residue prevents absent unless ruled irrelevant | `vac-assessment::evaluate_missing_code_proof`; `tests/fixtures/v19/missing-code/triad.json` | TV-Pass | End-to-end generator emission pending. |
| FG-readiness | explicit state reduction and trust wording; assessment cannot raise readiness | `vac-readiness`; compiler readiness summary | SV-Pass | Add v1.9 blocked/deprecated state model and trust wording output. |
| FG-assessment | findings cite deterministic evidence or valid missing-code proof; text cannot raise readiness | `vac-assessment`; assessment freshness scripts; missing-code proof triad | TV-Pass | Baseline debt persistence fixture still pending. |
| FG-adoption-friction | low-risk micro-slice and pause UX; low-risk typo avoids high-risk closeout | v1.5 runtime e2e fixtures | TV-Pending | v1.9 micro-slice classifier fixture pending. |

## Critical P0 acceptance closure checklist

| Acceptance item | Evidence | Status |
| --- | --- | --- |
| Duplicate `(session_id, seq)` rejected | Coverage script executes migration in temp SQLite and asserts duplicate insert fails. | TV-Pass |
| Stale decision cannot authorize current action | `vac-state::evaluate_runtime_decision_authorization`; unit test. | TV-Pass |
| Ghost state quarantine | `vac-state::classify_manifest_sync_record`; unit test. | TV-Pass |
| Derived trust recomputed at read time | `vac-state::derive_runtime_trust_at_read`; unit test. | TV-Pass |
| Subprocess `.vac/db/**` mutation detection | `vac-state::evaluate_subprocess_runtime_db_mutation`; unit test. | TV-Pass primitive; integration pending |
| Release doctor does not overclaim L1 local records | `vac-doctor::doctor_release_trust_language`; unit tests. | TV-Pass |
| Governance denominator/numerator reproducible | `vac-doctor::reduce_governance_window`; governance zero denominator and spec-window fixtures. | TV-Pass |
| Missing-code absence proof complete | `vac-assessment::evaluate_missing_code_proof`; triad fixture covers `absent`, `not_found_in_index`, `coverage_insufficient`. | TV-Pass |
| Bounded patch hard-deny `.vac/db/**` rejected | `vac-agent-loop::BoundRuntimeController::pre_patch_gate`; hard-deny JSON fixture. | TV-Pass |
| Span CRLF raw/normalized behavior deterministic | `vac-index::raw_span_sha256`; `normalized_text_fingerprint`; CRLF fixture. | TV-Pass |
| Doctor trust wording golden snapshots | `vac-doctor::doctor_trust_wording_golden_snapshots_match`; doctor-output JSON fixture. | TV-Pass |
| Full P0 acceptance | Not claimed. Pending groups remain. | TV-Pending |

## Remaining fixture closure order

1. Deterministic decision-classifier fixture for authority mutation, team-relevant, and ambiguous escalation floors.
2. Ownership fixtures for hidden, unowned, overclaimed, and shared-role paths.
3. DB-backed completion-lock fixture over real runtime journal state.
4. v1.9 micro-slice classifier/adoption-friction fixture.
5. Memory-does-not-authorize policy fixture.
6. Full doctor command output snapshots for every required gate, beyond trust wording.
