# VAC-Init P6 TUI Real-Data Adapters

This slice adds live workspace adapters for capability dashboard, approval popup data, and autopilot doctor monitor.

The existing TUI capability dashboard already reads control-plane reports; this slice adds deterministic adapter tests and report-derived validation.
