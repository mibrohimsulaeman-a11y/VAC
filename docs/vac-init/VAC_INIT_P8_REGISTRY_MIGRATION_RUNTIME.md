# VAC-Init P8 Registry Migration Runtime

This slice adds `vac registry migrate --dry-run|--apply` and `vac doctor migrations` wiring for checked migration records.

Compatibility kinds remain rejected by strict registry mode.
