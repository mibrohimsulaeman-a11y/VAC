# Scheduled Plan — 2026-05-26

## Priority stack
1. **Resolusi & Penyelarasan Temuan Audit Sentinel (Selesai)**: Menuntaskan kegagalan kompilasi pengujian unit (E0063), pendaftaran domain kapabilitas baru (`event_stream`, `external_agent_config`, `plugin_surface`), eliminasi alarm positif palsu identitas, serta sinkronisasi pemilik rute di `palette.yaml`. Status audit per 20:04 - 23:04 telah 100% Bersih (Clean).
2. **Pensiun Protokol & Dependensi Warisan (`vac_app_server` / Plan 31e & Plan 33)**: Menghapus dependensi Cargo `vac_app_server` dari manifest TUI dan mengamankan pemutusan total default local product path.
3. **Penyelesaian Dokumen Closeout & Bukti Defer/Delete**: Menyusun pemutakhiran berkas bukti integrasi dan transisi arsitektural ke fase berikutnya.

## Slice plan

### Slice 1 — Resolusi Kegagalan Kompilasi Unit Test `vac-local-runtime-owner` (CRITICAL)
- Goal: Mengatasi error E0063 pada berkas pengujian `local-runtime-owner/src/lib.rs` akibat field baru `vac_home`.
- Why: Struct `RuntimeExternalAgentConfigDetectCommand` di `command_bus.rs` ditambahkan field `vac_home: PathBuf`, namun inisialisasi di unit test `lib.rs:562` tidak menyertakannya.
- Files:
  - [lib.rs](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/local-runtime-owner/src/lib.rs)
- Steps:
  1. Buka inisialisasi unit test struct `RuntimeExternalAgentConfigDetectCommand` pada baris 562 di `lib.rs`.
  2. Tambahkan deklarasi `vac_home: PathBuf::new()`.
  3. Validasi kegunaan lewat compiler cargo.
- Validation:
  - `cargo check --manifest-path vac-rs/Cargo.toml --all-targets`
- Docs sync:
  - Tidak ada sinkronisasi dokumen eksternal langsung.
- Risks:
  - Risiko: Sangat rendah, hanya menyentuh lingkungan inisialisasi test internal.
  - Rollback: `git checkout -- vac-rs/local-runtime-owner/src/lib.rs`
- UX impact: Menjamin rantai integrasi lokal dan pengujian berkala tidak terhambat oleh kerusakan kompilasi.
- Owner/Coordination note: **Backend Engineer** (Telah selesai diselesaikan dan di-commit pada SHA `680c69b`).

### Slice 2 — Pengecualian Folder Bukti Rencana dari Pemindaian Identitas (WARNING)
- Goal: Menghapus kebisingan alarm positif palsu (*false-positive*) "duplicate TUI" pada berkas log di folder bukti rencana.
- Why: Log status git untracked di bawah folder `33-evidence` memuat referensi berkas proteksi TUI sehingga dideteksi oleh pemindai `identity_check.rs`.
- Files:
  - [identity_check.rs](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/core/src/control_plane/identity_check.rs)
- Steps:
  1. Buka `vac-rs/core/src/control_plane/identity_check.rs`.
  2. Daftarkan jalur `docs/workflow-control-plane/plans/33-evidence/**` ke konstanta array `IDENTITY_CHECK_EXEMPTIONS`.
- Validation:
  - `./vac-rs/target/debug/vac doctor workflow .`
- Docs sync:
  - Menstabilkan status kepatuhan workflow CI lokal kembali ke warna hijau (Pass).
- Risks:
  - Risiko: Pengecualian wildcard berpotensi menyamarkan penyalahgunaan identitas di masa mendatang jika file non-evidence diletakkan di sana. Namun risiko ini kecil untuk direktori khusus evidence.
  - Rollback: `git checkout -- vac-rs/core/src/control_plane/identity_check.rs`
- UX impact: Mengurangi kebisingan diagnostik dan menjaga integritas laporan harian tetap akurat.
- Owner/Coordination note: **Quality Sentinel** (Telah selesai diselesaikan dan di-commit pada SHA `0c9b8c5`).

### Slice 3 — Deklarasi Kepemilikan Domain `event_stream.rs`, `external_agent_config.rs`, & `plugin_surface.rs` (WARNING)
- Goal: Mendaftarkan modul sumber baru di bawah `local-runtime-owner` ke manifest kapabilitas resmi agar lolos diagnostik registri.
- Why: Berkas-berkas fungsional baru telah ditambahkan ke kode sumber, namun belum diklaim oleh target kepemilikan statis manapun di manifest kapabilitas.
- Files:
  - [local_runtime_owner.yaml](file:///home/emp/Documents/VAC/vastar-agentic-cli/.vac/capabilities/local_runtime_owner.yaml)
- Steps:
  1. Buka berkas manifest `.vac/capabilities/local_runtime_owner.yaml`.
  2. Daftarkan modul `event_stream`, `external_agent_config`, dan `plugin_surface` di bawah bagian `ownership.targets`.
- Validation:
  - `./vac-rs/target/debug/vac doctor registry .`
- Docs sync:
  - Menyelaraskan dokumen kapabilitas statis dengan perkembangan komponen kode fisik terbaru.
- Risks:
  - Risiko: Sangat rendah, murni pemutakhiran administratif metadata YAML.
  - Rollback: `git checkout -- .vac/capabilities/local_runtime_owner.yaml`
- UX impact: Dashboard kapabilitas merefleksikan cakupan kepemilikan modul secara 100% akurat.
- Owner/Coordination note: **Registry Maintainer** (Telah selesai diselesaikan dan di-commit pada SHA `b8a46a3` & integrasi).

### Slice 4 — Sinkronisasi Pemilik Rute Permukaan Palette (`palette.yaml`) dengan Capability (WARNING)
- Goal: Mengeliminasi warning pindaian registri akibat mismatch pemilik rute di `palette.yaml`.
- Why: Label pemilik rute `open_local_runtime_owner` (indeks 7), `open_runtime_approval_bridge` (indeks 8), dan `open_tui_session_runtime` (indeks 15) berbeda dengan entitas pemilik kapabilitas resmi.
- Files:
  - [palette.yaml](file:///home/emp/Documents/VAC/vastar-agentic-cli/.vac/surfaces/palette.yaml)
- Steps:
  1. Perbarui nilai `owner` rute permukaan palette agar menunjuk ke file implementasi kapabilitas yang valid dan resmi.
  2. Jalankan pindaian harian registri untuk verifikasi.
- Validation:
  - `./vac-rs/target/debug/vac doctor registry .`
- Docs sync:
  - Penyelarasan deklarasi manifest statis dengan rute visual command palette.
- Risks:
  - Risiko: Nihil, tidak memengaruhi alur kontrol runtime.
  - Rollback: `git checkout -- .vac/surfaces/palette.yaml`
- UX impact: Membersihkan konsol log diagnostik bagi pengembang lokal.
- Owner/Coordination note: **Frontend Integration Architect** (Telah diselesaikan dan disinkronkan).

### Slice 5 — Pemutusan Dependensi Cargo Warisan `vac_app_server` dan Penutupan Plan 33 (Langkah Lanjutan)
- Goal: Menghapus dependensi Cargo `vac-app-server` dari pustaka TUI pasca pensiunnya protokol `core-skills`.
- Why: Mengamankan default local product path agar bersih dari dependensi warisan backend lama sebelum mengklaim bukti defer/delete harian.
- Files:
  - [Cargo.toml](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/tui/Cargo.toml)
  - Dokumen closeout Plan 33 & Phase 00
- Steps:
  1. Hapus entri dependensi `vac-app-server`, `vac-app-server-client`, dan `vac-app-server-protocol` dari manifest `vac-rs/tui/Cargo.toml`.
  2. Lakukan pemeriksaan integritas kompilasi dan uji suite.
  3. Validasi arsitektur dependensi bebas dari keterkaitan backend lama.
- Validation:
  - `cargo check --manifest-path vac-rs/Cargo.toml -p vac-surface-tui`
  - `cargo tree -p vac-surface-cli -i vac-app-server --edges normal,build` (Hasil pencarian harus kosong)
- Docs sync:
  - Mutakhirkan `docs/workflow-control-plane/plans/33-app-server-cargo-retirement-delete-defer-proof.md` dan berkas PHASE00 harian.
- Risks:
  - Risiko: Kerusakan kompilasi pada TUI jika masih terdapat sisa impor fungsional tersembunyi.
  - Rollback: `git restore vac-rs/tui/Cargo.toml`
- UX impact: Peningkatan efisiensi waktu kompilasi dan pengurangan berat biner produk secara signifikan.
- Owner/Coordination note: **Core Architect & Development Team**.

## Deferred / blocked
- **Pembersihan Fisik Crate `vac_app_server`**: Ditangguhkan (deferred) sebagai kapabilitas non-default karena beberapa repositori operator sekunder masih memilikinya sebagai opsi opsional.
- **Eksekusi Pengujian Penuh TTY/PTY secara manual**: Tetap diblokir oleh operator (`BLOCKED-OPERATOR`) karena keterbatasan lingkungan non-interaktif sandbox.

## Coordination notes
- **Preservasi Progres Aktif**: Meskipun sebagian besar tugas telah diselesaikan dan di-commit hari ini, pastikan tidak ada eksekusi perintah destruktif (`git clean -fd`, `git reset --hard`) dijalankan di workspace tanpa koordinasi.
- **Fast Validation Pattern (Rust)**: Lakukan kompilasi sekali (`cargo build`) lalu jalankan biner `./target/debug/vac` secara langsung untuk menguji subsistem guna mempercepat pengerjaan harian.
