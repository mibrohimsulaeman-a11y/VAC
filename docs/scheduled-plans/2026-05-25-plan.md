# Scheduled Plan — 2026-05-25

## Priority stack
1. **Preservasi Progres Kerja Aktif (Dirty Work)**: Menjaga integritas ~155+ berkas termodifikasi dan untracked (seperti modul `local-runtime-owner` dan `approval_store.rs`) di working tree.
2. **Penyelarasan Kepemilikan Rute Command Palette (palette.yaml)**: Memperbaiki inkonsistensi penamaan pemilik (*owner*) rute permukaan command palette dengan pemilik kapabilitas di manifest untuk rute local runtime owner, runtime approval bridge, dan TUI session.
3. **Pendaftaran Kepemilikan Domain event_stream.rs**: Mendaftarkan domain modul baru `vac-local-runtime-owner/event_stream` ke dalam manifest kapabilitas `local_runtime_owner.yaml` untuk mengeliminasi warning pindaian registri.
4. **Pengecualian Folder Bukti Rencana 33-evidence**: Menambahkan jalur bukti rencana `docs/workflow-control-plane/plans/33-evidence/**` ke dalam pengecualian `IDENTITY_CHECK_EXEMPTIONS` di `identity_check.rs` untuk menghilangkan alarm positif palsu "duplicate TUI".

## Slice plan

### Slice 1 — Preservasi Progres Kerja Aktif & Validasi Workspace
- Goal: Menjamin keberlanjutan modifikasi masif (~155+ file) tanpa adanya risiko penghancuran kode (*destructive commands*).
- Why: **Root Cause** berupa adanya progres migrasi bernilai tinggi di working tree, seperti crate baru `vac-local-runtime-owner` dan berkas untracked `approval_store.rs` yang belum di-commit. Perintah destruktif seperti `git reset --hard` atau `git clean -fd` dilarang keras dijalankan.
- Files:
  - Seluruh working tree (khususnya berkas-berkas untracked/termodifikasi di `vac-rs/` dan `.vac/`)
- Steps:
  1. Identifikasi dan verifikasi seluruh status modifikasi di working tree dengan perintah non-destruktif.
  2. Pastikan tidak ada perintah pembersihan otomatis (git clean/reset) yang berjalan di pipeline lokal maupun CI.
  3. Pertahankan isolasi dependensi Cargo donor agar tetap lulus gerbang migrasi (`scripts/check-donor-status.sh`).
- Validation:
  - `git status`
  - `cargo check --manifest-path vac-rs/Cargo.toml -p vac-surface-cli`
  - `bash scripts/check-donor-status.sh all`
- Docs sync:
  - Tidak ada berkas dokumentasi yang berubah langsung pada slice ini.
- Risks:
  - Risiko: Kehilangan progres pengerjaan aktif senilai puluhan jam kerja jika perintah destruktif dijalankan secara sengaja atau tidak sengaja.
  - Rollback: Tidak ada rollback karena tujuannya adalah memelihara kondisi saat ini (*no-op safety*).
- UX impact: Menjamin fitur-fitur local runtime baru (Plan 24-33) yang sedang dikembangkan dapat diselesaikan dengan aman.
- Owner/Coordination: **Core Architect** & **Development Team** untuk koordinasi perlindungan working tree.

### Slice 2 — Penyelarasan Pemilik Rute Permukaan Command Palette (`palette.yaml`)
- Goal: Menghilangkan warning perbedaan pemilik rute permukaan command palette dengan pemilik kapabilitas terkait pada pindaian `vac doctor registry`.
- Why: **Root Cause** berupa inkonsistensi penamaan pemilik (*owner*) pada konfigurasi `.vac/surfaces/palette.yaml` dibandingkan dengan pemilik kapabilitas (*owner*) yang didefinisikan secara resmi di manifest kapabilitas `.vac/capabilities/` untuk modul local runtime, approval bridge, dan TUI session.
- Files:
  - [palette.yaml](file:///home/emp/Documents/VAC/vastar-agentic-cli/.vac/surfaces/palette.yaml)
- Steps:
  1. Buka `.vac/surfaces/palette.yaml` dan temukan rute `open_local_runtime_owner` (indeks 7). Ubah `owner: vac-local-runtime-owner/startup` menjadi `owner: vac-rs/local-runtime-owner/src/startup.rs`.
  2. Pada rute `open_runtime_approval_bridge` (indeks 8), ubah `owner: vac-core/local_runtime.approval` menjadi `owner: vac-rs/local-runtime-owner/src/command_bus.rs`.
  3. Pada rute `open_tui_session_runtime` (indeks 15), ubah `owner: vac-tui/local_runtime_session` menjadi `owner: vac-rs/tui/src/app_server_session.rs`.
  4. Simpan berkas dan jalankan validasi registri.
- Validation:
  - `./vac-rs/target/debug/vac doctor registry .`
- Docs sync:
  - Menyelaraskan status pemilik di `.vac/surfaces/palette.yaml` agar sinkron dengan manifest `.vac/capabilities/local_runtime_owner.yaml`, `.vac/capabilities/runtime_approval_bridge.yaml`, dan `.vac/capabilities/tui_session_runtime.yaml`.
- Risks:
  - Risiko: Sangat rendah, karena hanya memodifikasi label visual metadata kepemilikan pada file konfigurasi permukaan.
  - Rollback: `git checkout -- .vac/surfaces/palette.yaml`
- UX impact: Menghilangkan kebisingan warning kepemilikan registri saat operator memindai repositori menggunakan CLI, memastikan visualisasi dashboard palette bersih dan konsisten.
- Owner/Coordination: **Core Architect** & **Registry Maintainer**.

### Slice 3 — Deklarasi Kepemilikan Domain Modul `event_stream.rs`
- Goal: Mendaftarkan modul sumber baru `event_stream.rs` di bawah crate `vac-local-runtime-owner` ke dalam manifest kapabilitas agar lolos validasi kepemilikan domain.
- Why: **Root Cause** berupa adanya berkas modul baru `event_stream.rs` yang telah ditambahkan di bawah crate `vac-local-runtime-owner` namun belum diklaim oleh target kepemilikan kapabilitas mana pun dalam `.vac/capabilities/`.
- Files:
  - [local_runtime_owner.yaml](file:///home/emp/Documents/VAC/vastar-agentic-cli/.vac/capabilities/local_runtime_owner.yaml)
- Steps:
  1. Buka berkas `.vac/capabilities/local_runtime_owner.yaml`.
  2. Temukan blok `ownership.targets`.
  3. Tambahkan entri baru di bawah daftar targets:
     ```yaml
     - crate_name: vac-local-runtime-owner
       module: event_stream
     ```
  4. Simpan berkas manifest dan jalankan pemeriksaan registri.
- Validation:
  - `./vac-rs/target/debug/vac doctor registry .`
- Docs sync:
  - Memperbarui manifest kapabilitas [.vac/capabilities/local_runtime_owner.yaml](file:///home/emp/Documents/VAC/vastar-agentic-cli/.vac/capabilities/local_runtime_owner.yaml) agar mencakup modul Rust fisik baru.
- Risks:
  - Risiko: Sangat rendah, hanya menambahkan metadata kepemilikan dalam berkas YAML.
  - Rollback: `git checkout -- .vac/capabilities/local_runtime_owner.yaml`
- UX impact: Visualisasi dashboard kepemilikan kode akan mencerminkan secara akurat bahwa modul event stream dimiliki sepenuhnya oleh kapabilitas Local Runtime Owner, melengkapi data integritas arsitektur.
- Owner/Coordination: **Backend Engineer** & **Registry Maintainer**.

### Slice 4 — Pengecualian Folder Bukti Rencana dari Pemindaian Identitas
- Goal: Menghilangkan alarm positif palsu (*false-positive cascade*) bertema istilah terlarang "duplicate TUI" pada catatan bukti integrasi lokal.
- Why: **Root Cause** berupa berkas catatan bukti integrasi untracked (`git-status-short.txt`) di bawah folder `docs/workflow-control-plane/plans/33-evidence/...` yang merekam log git status yang mengandung referensi berkas unique TUI (`maintenance.no-duplicate-tui.yaml` dan `no_duplicate_tui.rs`). Karena folder ini berada di bawah `docs` yang merupakan bagian dari scan roots, pemindai di `identity_check.rs` memindainya secara tidak sengaja dan memicu alarm.
- Files:
  - [identity_check.rs](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/core/src/control_plane/identity_check.rs)
- Steps:
  1. Buka berkas `vac-rs/core/src/control_plane/identity_check.rs`.
  2. Temukan konstanta array `IDENTITY_CHECK_EXEMPTIONS`.
  3. Daftarkan folder bukti rencana ke dalam daftar pengecualian:
     ```rust
     IdentityExemption {
         path: "docs/workflow-control-plane/plans/33-evidence/**",
         reason: "Evidence files (like git status dumps) quote historical or uncommitted file names with forbidden terms.",
         expires_when: "Evidence directory is archived or deleted",
     },
     ```
  4. Simpan berkas dan jalankan verifikasi `vac doctor workflow .` untuk memastikan alarm positif palsu tersebut hilang.
- Validation:
  - `cargo test --manifest-path vac-rs/Cargo.toml -p vac-core control_plane::identity_check::tests`
  - `./vac-rs/target/debug/vac doctor workflow .`
- Docs sync:
  - Tidak ada dokumen eksternal yang diubah langsung, namun gerbang integrasi CI/CD lokal kembali berstatus hijau (*pass*).
- Risks:
  - Risiko: Menambahkan pengecualian dengan wildcard `**` pada folder evidence dapat menyembunyikan pelanggaran riil yang sengaja diletakkan di sana. Namun, karena folder ini didedikasikan hanya untuk catatan baseline dan jalannya evidence dump, risiko ini dapat diabaikan.
  - Rollback: `git checkout -- vac-rs/core/src/control_plane/identity_check.rs`
- UX impact: Menghilangkan kebisingan peringatan identitas (WARNING) pada pindaian rutin, menjaga repositori tetap patuh 100% pada aturan arsitektur VAC.
- Owner/Coordination: **Security & Quality Sentinel** & **Development Team**.

## Deferred / blocked
- **Pembersihan fisik dan penghapusan crate donor**: Ditangguhkan hingga modul backend esensial selesai di-port sepenuhnya (Plan 00E/Plan 24-33).
- **Pengujian penuh end-to-end PTY TUI secara manual**: Ditangguhkan karena lingkungan sandbox bersifat non-interactive dan tidak mengizinkan alokasi buffer TTY interaktif (berstatus `BLOCKED-OPERATOR`).
- **Eksekusi pengujian penuh `cargo test --all`**: Ditangguhkan guna mencegah terjadinya cargo file locks dan pemborosan waktu kompilasi selama audit berkala.

## Coordination notes
- **Preservasi Dirty Work**: Seluruh perubahan uncommitted (~155 file termodifikasi/untracked, termasuk crate `local-runtime-owner`, `approval_store.rs`, dll.) **wajib dipertahankan sepenuhnya**. Jangan jalankan `git clean -fd`, `git checkout --`, atau `git reset`.
- **Fast Validation Pattern (Rust)**: Selalu gunakan pola kompilasi sekali (`cargo build`), dilanjutkan dengan eksekusi biner langsung di `./vac-rs/target/debug/vac doctor` untuk menghindari kompilasi ulang Cargo yang lambat.
- **Kepatuhan 100% Identity Check**: Pastikan pengecualian scanner diterapkan dengan hati-hati agar tidak mengabaikan pelanggaran identitas fungsional di luar direktori bukti rencana.
