# Scheduled Plan — 2026-05-24

## Priority stack
1. **Preservasi Progres Kerja Aktif (Dirty Work)**: Menjaga integritas 155+ berkas termodifikasi dan untracked (seperti modul `local-runtime-owner` dan `approval_store.rs`) di working tree.
2. **Dekopling Dependensi Legacy Transport (Plan 00F)**: Melanjutkan pembersihan dan dekopling bertahap terhadap dependensi legacy `vac-app-server` pada modul TUI.
3. **Penyelesaian Marker TODO Sesi**: Menyelesaikan marker TODO helper akses sesi sementara di `local_runtime_session.rs` demi menghilangkan technical debt.
4. **Persistensi Alur Kerja SQLite**: Melanjutkan integrasi database persistensi pada berkas untracked `approval_store.rs`.
5. **Penyelarasan & Hardening Dokumen Migrasi Donor**: Melakukan pembaharuan status kemajuan pada `DONOR_STATUS_BOARD.md` dan sinkronisasi data modular.

## Slice plan

### Slice 1 — Preservasi Progres Kerja Aktif & Validasi Workspace
- Goal: Menjamin keberlanjutan modifikasi masif (155+ file) tanpa adanya risiko penghancuran kode (destructive commands).
- Why: **Root Cause** berupa adanya progres migrasi bernilai tinggi di working tree, seperti crate baru `vac-local-runtime-owner` dan berkas untracked `approval_store.rs` yang belum di-commit. Perintah destruktif seperti `git reset --hard` atau `git clean -fd` dilarang keras dijalankan.
- Files:
  - Seluruh working tree (khususnya berkas-berkas untracked/termodifikasi di `vac-rs/` dan `.vac/`)
- Steps:
  1. Identifikasi dan verifikasi seluruh status modifikasi di working tree dengan perintah non-destruktif.
  2. Pastikan tidak ada perintah pembersihan otomatis (git clean/reset) yang berjalan di pipeline lokal.
  3. Pertahankan isolasi dependensi Cargo donor agar tetap lulus gerbang migrasi (`scripts/check-donor-status.sh`).
- Validation:
  - `git status`
  - `cargo check --manifest-path vac-rs/Cargo.toml -p vac-surface-cli`
  - `bash scripts/check-donor-status.sh all`
- Docs sync:
  - Tidak ada berkas dokumentasi yang berubah langsung pada slice ini.
- Risks:
  - Risiko: Kehilangan progres pengerjaan aktif senilai puluhan jam kerja jika perintah destruktif dijalankan secara sengaja atau tidak sengaja.
  - Rollback: Tidak ada rollback karena tujuannya adalah memelihara kondisi saat ini (no-op safety).
- UX impact: Menjamin fitur-fitur local runtime baru (Plan 24-33) yang sedang dikembangkan dapat diselesaikan dengan aman.
- Owner/Coordination: **Core Architect** & **Development Team** untuk koordinasi perlindungan working tree.

### Slice 2 — Dekopling Dependensi Legacy Transport `vac-tui → vac-app-server` (Plan 00F)
- Goal: Membersihkan ketergantungan langsung modul TUI terhadap legacy transport app-server untuk mempercepat waktu kompilasi dan startup.
- Why: **Root Cause** berupa temuan audit arsitektur internal di mana TUI masih memiliki ketergantungan Cargo langsung pada crate-crate legacy (`vac-app-server`, `vac-app-server-client`, `vac-app-server-protocol`). Meskipun fungsionalitas telah dibatasi ke dalam batas `LocalRuntimeSession`, Cargo edges ini perlu didekopling penuh.
- Files:
  - [vac-rs/tui/Cargo.toml](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/tui/Cargo.toml)
  - [vac-rs/tui/src/local_runtime_session.rs](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/tui/src/local_runtime_session.rs)
  - [vac-rs/tui/src/session_protocol.rs](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/tui/src/session_protocol.rs)
- Steps:
  1. Review sisa penggunaan adapter app-server di `vac-rs/tui/src/app_server_session.rs`.
  2. Isolasi dan pindahkan DTO tersisa ke `vac-protocol` atau `vac-thread-store`.
  3. Lakukan pengujian fungsionalitas TUI dengan mock local runtime.
  4. Hapus dependensi Cargo legacy dari `vac-rs/tui/Cargo.toml` secara bertahap saat adapter lokal telah mandiri sepenuhnya.
- Validation:
  - `./vac-rs/target/debug/vac doctor architecture .`
  - `cargo check --manifest-path vac-rs/Cargo.toml -p vac-surface-tui --tests`
- Docs sync:
  - Perbarui [00F-tui-legacy-transport-retirement.md](file:///home/emp/Documents/VAC/vastar-agentic-cli/docs/workflow-control-plane/plans/00F-tui-legacy-transport-retirement.md) untuk mencatat status penyelesaian.
- Risks:
  - Risiko: Pemutusan dependensi yang terlalu dini dapat merusak kompatibilitas ke belakang (backward compatibility) dengan modul app-server.
  - Rollback: `git checkout -- vac-rs/tui/Cargo.toml` untuk memulihkan dependensi awal.
- UX impact: Startup TUI menjadi jauh lebih cepat, konsumsi memori biner lebih rendah, dan sistem berjalan 100% lokal secara mandiri.
- Owner/Coordination: **Frontend Lead** & **Core Architecture Lead** untuk integrasi modul sesi lokal.

### Slice 3 — Pembersihan Technical Debt: Resolusi Marker TODO di `local_runtime_session.rs`
- Goal: Menyelesaikan marker TODO helper akses sesi (`TODO(local-runtime-owner)`) agar kode di `local_runtime_session.rs` bersih dari hutang teknis.
- Why: **Root Cause** berupa penambahan marker TODO sementara pada helper akses sesi pengujian masa depan di modul TUI (`local_runtime_session.rs:166`). Marker ini harus dibersihkan atau ditransformasikan ke pelacakan formal agar tidak mengendap menjadi dead code.
- Files:
  - [vac-rs/tui/src/local_runtime_session.rs](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/tui/src/local_runtime_session.rs)
- Steps:
  1. Periksa fungsionalitas helper akses sementara pada `local_runtime_session.rs:166`.
  2. Jika helper tersebut hanya untuk pengujian, tandai dengan anotasi `#[cfg(test)]`.
  3. Hapus marker komentar `TODO` setelah helper terintegrasi secara formal ke dalam interface test suite.
- Validation:
  - `cargo test --manifest-path vac-rs/Cargo.toml -p vac-surface-tui`
- Docs sync:
  - Tidak ada sinkronisasi dokumen eksternal yang diwajibkan.
- Risks:
  - Risiko: Sangat rendah karena hanya mengubah dokumentasi internal/penataan kode helper.
  - Rollback: `git checkout -- vac-rs/tui/src/local_runtime_session.rs`
- UX impact: Kode internal TUI menjadi lebih bersih, rapi, dan mudah dirawat jangka panjang tanpa noise marker TODO.
- Owner/Coordination: **Frontend Lead** untuk pembersihan file-file TUI.

### Slice 4 — Kelanjutan Integrasi Persistensi Alur Kerja di `approval_store.rs`
- Goal: Menyelesaikan implementasi database persistensi persetujuan operator untuk alur kerja di `approval_store.rs`.
- Why: **Root Cause** berupa kebutuhan engine workflow runner untuk memiliki penyimpanan status persetujuan yang persisten agar status alur kerja tidak hilang saat TUI atau CLI di-restart. Berkas `approval_store.rs` saat ini masih berupa draf untracked di working tree.
- Files:
  - [vac-rs/core/src/control_plane/approval_store.rs](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/core/src/control_plane/approval_store.rs)
  - [vac-rs/core/src/control_plane/mod.rs](file:///home/emp/Documents/VAC/vastar-agentic-cli/vac-rs/core/src/control_plane/mod.rs)
- Steps:
  1. Selesaikan implementasi penyimpanan/pembacaan status persetujuan menggunakan backend SQLite di `approval_store.rs`.
  2. Hubungkan `ApprovalStore` dengan engine lifecycle `approval_lifecycle.rs` di dalam crate `vac-core`.
  3. Lakukan pengujian unit untuk memverifikasi proses simpan-dan-muat (save-and-load) data persetujuan.
- Validation:
  - `cargo test --manifest-path vac-rs/Cargo.toml -p vac-core control_plane::approval_store_tests`
  - `./vac-rs/target/debug/vac doctor workflow .`
- Docs sync:
  - Perbarui [plans/14-workflow-runner-integration.md](file:///home/emp/Documents/VAC/vastar-agentic-cli/docs/workflow-control-plane/plans/14-workflow-runner-integration.md) (atau rencana terkait) untuk mencatat kelulusan fungsionalitas persistensi.
- Risks:
  - Risiko: Kesalahan penulisan skema SQL dapat merusak database `state.db` lokal.
  - Rollback: Backup `state.db` sebelum uji coba, dan hapus berkas jika terjadi korupsi skema.
- UX impact: Alur kerja operator menjadi sangat tangguh; status persetujuan yang tertunda atau disetujui akan dipertahankan dengan aman meskipun aplikasi mengalami restart.
- Owner/Coordination: **Core Architecture Lead** & **Database Specialist**.

### Slice 5 — Sinkronisasi Peta Kapabilitas & Board Status Migrasi Donor
- Goal: Memperbarui dokumen rencana migrasi donor dan board status agar sejalan dengan modifikasi baru pada local runtime owner.
- Why: **Root Cause** berupa proses pemindahan kode backend dari donor ke root product secara paralel. Board status migrasi donor dan peta kapabilitas harus tetap sinkron dengan inventori source code riil agar tidak memicu deteksi ketidakselarasan.
- Files:
  - [DONOR_STATUS_BOARD.md](file:///home/emp/Documents/VAC/vastar-agentic-cli/docs/donor-migration/DONOR_STATUS_BOARD.md)
  - [CAPABILITY_MAP.md](file:///home/emp/Documents/VAC/vastar-agentic-cli/docs/product/CAPABILITY_MAP.md)
- Steps:
  1. Jalankan skrip status donor untuk melihat persentase cakupan modular terbaru.
  2. Perbarui status domain plans di `DONOR_STATUS_BOARD.md` dari `in_progress` ke `MIGRATED` jika modul backend terkait telah dipromosikan ke root.
  3. Sinkronkan peta kapabilitas di `CAPABILITY_MAP.md` dengan status terbaru dari visual dashboard kapabilitas.
- Validation:
  - `bash scripts/check-donor-status.sh status`
  - `./vac-rs/target/debug/vac doctor registry .`
- Docs sync:
  - Memperbarui file dokumentasi migrasi secara berkala agar mencerminkan progress riil.
- Risks:
  - Risiko: Dokumentasi yang tidak sinkron dapat membingungkan pengembang mengenai status modul mana yang aman dikonsumsi.
  - Rollback: `git checkout` pada file `.md` yang terdampak.
- UX impact: Transparansi progres proyek meningkat pesat, memberikan gambaran yang akurat tentang tingkat kesiapan produksi VAC.
- Owner/Coordination: **Documentation Maintainer** & **Core Architect**.

## Deferred / blocked
- **Pembersihan fisik dan penghapusan crate donor**: Ditangguhkan hingga modul backend esensial selesai di-port sepenuhnya (Plan 00E/Plan 24-33).
- **Pengujian penuh end-to-end PTY TUI secara manual**: Ditangguhkan karena lingkungan sandbox bersifat non-interactive dan tidak mengizinkan alokasi buffer TTY interaktif (berstatus `BLOCKED-OPERATOR`).
- **Eksekusi pengujian penuh `cargo test --all`**: Ditangguhkan guna mencegah terjadinya cargo file locks dan pemborosan waktu kompilasi selama audit berkala.

## Coordination notes
- **Preservasi Dirty Work**: Seluruh perubahan uncommitted (~155 file termodifikasi/untracked, termasuk crate `local-runtime-owner`, `approval_store.rs`, dll.) **wajib dipertahankan sepenuhnya**. Jangan jalankan `git clean -fd`, `git checkout --`, atau `git reset`.
- **Isolasi Donor Tetap 100%**: Jaga ketat ketiadaan kebocoran dependensi kargo donor dengan rutin menjalankan `bash scripts/check-donor-status.sh`.
- **Pola Validasi Cepat Rust**: Selalu gunakan pola kompilasi sekali (`cargo build`), dilanjutkan dengan eksekusi binary langsung di `./vac-rs/target/debug/vac doctor` untuk menghindari kompilasi ulang Cargo yang memakan waktu lama.
