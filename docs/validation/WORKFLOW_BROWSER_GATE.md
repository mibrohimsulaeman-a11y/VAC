# Workflow Browser Gate

## Goal

Verify workflows are visible and inspectable.

## Required checks

```text
/workflow opens
workflow list renders
workflow steps render
missing capability diagnostic appears
policy/approval gates are visible
running workflow progress is visible when supported
```

## Cannot claim if failed

```text
workflow-control-plane usable
workflow runner product-ready
```
