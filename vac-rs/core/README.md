# VAC core compatibility area

This directory is intentionally a compatibility/facade area during the migration. Product logic lives under `vac-rs/crates/**`. Delete this area once downstream imports no longer need a core facade.
