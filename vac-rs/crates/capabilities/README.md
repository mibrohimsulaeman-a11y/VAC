# VAC capability crates

Capability crates are admitted only when feature-owned logic cannot live in foundation/control-plane/runtime/surfaces/providers/integrations. Every capability crate must have a `.vac/capabilities/*.yaml` owner and validation gates.
