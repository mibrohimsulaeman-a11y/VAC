vac-rs/core/tests/suite/otel.rs:    let logs = String::from_utf8(buffer.lock().unwrap().clone()).unwrap();
vac-rs/core/tests/suite/otel.rs:    let logs = String::from_utf8(buffer.lock().unwrap().clone()).unwrap();
vac-rs/core/tests/suite/otel.rs:    let logs = String::from_utf8(buffer.lock().unwrap().clone()).unwrap();
vac-rs/core/tests/common/responses.rs:        let requests = self.requests.lock().unwrap();
vac-rs/core/tests/common/responses.rs:        self.requests.lock().unwrap().clone()
vac-rs/core/tests/common/responses.rs:        self.requests.lock().unwrap().last().cloned()
vac-rs/core/tests/common/responses.rs:        self.connections.lock().unwrap().clone()
vac-rs/core/tests/common/responses.rs:        let connections = self.connections.lock().unwrap();
vac-rs/core/tests/common/responses.rs:        self.handshakes.lock().unwrap().clone()
vac-rs/core/tests/common/responses.rs:        if self.handshakes.lock().unwrap().len() >= expected {
vac-rs/core/tests/common/responses.rs:            if self.handshakes.lock().unwrap().len() >= expected {
vac-rs/core/tests/common/responses.rs:        let handshakes = self.handshakes.lock().unwrap();
vac-rs/core/tests/common/responses.rs:        self.requests.lock().unwrap().clone()
vac-rs/core/tests/common/responses.rs:        let requests = self.requests.lock().unwrap();
vac-rs/core/tests/common/responses.rs:        self.requests.lock().unwrap().push(request.clone());
vac-rs/core/tests/common/responses.rs:                let mut pending = connections.lock().unwrap();
vac-rs/core/tests/common/responses.rs:                handshake_log.lock().unwrap().push(WebSocketHandshake {
vac-rs/core/tests/common/responses.rs:                let mut log = requests.lock().unwrap();
vac-rs/core/tests/common/responses.rs:                    let mut log = requests.lock().unwrap();
vac-rs/core/tests/common/responses.rs:            if connections.lock().unwrap().is_empty() {
vac-rs/crates/providers/provider-http/src/endpoint/realtime_call.rs:            *self.last_request.lock().unwrap() = Some(req);
vac-rs/crates/providers/provider-http/src/endpoint/realtime_call.rs:        let request = transport.last_request.lock().unwrap().clone().unwrap();
vac-rs/crates/providers/provider-http/src/endpoint/realtime_call.rs:        let request = transport.last_request.lock().unwrap().clone().unwrap();
vac-rs/crates/providers/provider-http/src/endpoint/realtime_call.rs:        let request = transport.last_request.lock().unwrap().clone().unwrap();
vac-rs/crates/providers/provider-http/src/endpoint/realtime_call.rs:        let request = transport.last_request.lock().unwrap().clone().unwrap();
vac-rs/crates/providers/provider-http/src/endpoint/models.rs:            *self.last_request.lock().unwrap() = Some(req);
vac-rs/crates/surfaces/tui/src/onboarding/auth/headless_chatgpt_login.rs:            &*sign_in_state.read().unwrap(),
vac-rs/crates/surfaces/tui/src/onboarding/auth/headless_chatgpt_login.rs:            &*sign_in_state.read().unwrap(),
vac-rs/crates/surfaces/tui/src/onboarding/auth/headless_chatgpt_login.rs:            &*sign_in_state.read().unwrap(),
vac-rs/crates/surfaces/tui/src/onboarding/auth/headless_chatgpt_login.rs:            error.read().unwrap().as_deref(),
vac-rs/crates/surfaces/tui/src/onboarding/auth/headless_chatgpt_login.rs:            &*sign_in_state.read().unwrap(),
vac-rs/crates/surfaces/tui/src/onboarding/auth/headless_chatgpt_login.rs:        assert_eq!(*error.read().unwrap(), None);
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:        self.tags.lock().unwrap().extend(visitor.tags);
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:        *self.event_count.lock().unwrap() += 1;
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    let tags = tags.lock().unwrap().clone();
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    assert_eq!(*event_count.lock().unwrap(), 1);
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    let tags = tags.lock().unwrap().clone();
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    assert_eq!(*event_count.lock().unwrap(), 1);
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    let tags = tags.lock().unwrap().clone();
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    assert_eq!(*event_count.lock().unwrap(), 2);
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    let tags = tags.lock().unwrap().clone();
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    assert_eq!(*event_count.lock().unwrap(), 1);
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    let tags = tags.lock().unwrap().clone();
vac-rs/crates/capabilities/release/src/core_migrated/util_tests.rs:    assert_eq!(*event_count.lock().unwrap(), 2);
