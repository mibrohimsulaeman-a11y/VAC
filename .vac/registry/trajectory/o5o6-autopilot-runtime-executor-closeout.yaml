schema_version: 1
kind: trajectory
id: trajectory.o5o6-autopilot-runtime-executor-closeout
created_at: '2026-06-01T00:00:00Z'
plan_id: plan.o5o6.autopilot-runtime-executor-closeout
capability: vac.autopilot.scheduler
summary: Close latest runtime-readiness audit findings for the /runtime surface by replacing status-only autopilot behavior with workflow_runner-backed execution and live operator-console navigation/refresh.
steps:
  - id: audit-read
    status: done
    note: Latest audit confirmed writer/action wiring existed but scheduler remained declarative, actions mutated YAML only, tabs were not navigable, and refresh was keypress-only.
  - id: scheduler-executor
    status: done
    note: Scheduler now loads workflow manifests, writes running state, executes via execute_workflow_manifest, writes terminal state, and appends run evidence.
  - id: operator-console-live
    status: done
    note: Operator console now supports Tab/arrow navigation and pre-draw refresh ticks.
  - id: gates
    status: done
    note: Static gates now reject status-only scheduler and non-navigable console regressions.
truth:
  raw_chain_of_thought_excluded: true
  tv_status: TV-Pending
