# Surfaces (authored — routes only)

A surface binds operator-facing routes to capabilities.

- AUTHORED: `routes` only.
- DERIVED: the per-surface `capabilities:` coverage list is regenerated into
  `derived/surface-coverage.yaml` by `vac scan` (v1 hand-listed 60+ ids here — that rotted instantly).
