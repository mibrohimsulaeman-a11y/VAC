schema_version: 2
kind: capability
id: vac.control_plane.spec_sync
title: VAC SpecSync
readiness:
  declared: partial
  computed: partial
  effective: partial
  reason: "sandbox static implementation; TV cargo gates pending"
  blockers:
    - tv_cargo_build_pending
  last_checked_at: "2026-06-10T03:40:42Z"
owner:
  crate: vac-spec-sync
  module: root
ownership:
  targets:
    - kind: path
      include:
        - vac-rs/crates/control-plane/vac-spec-sync/**
      exclude:
        - vac-rs/crates/control-plane/vac-spec-sync/target/**
policy:
  risk: medium
  mutates_files: true
  network: false
  redaction: true
  approval_required_for:
    - filesystem_delete
    - execute_process
surfaces:
  cli:
    enabled: true
    commands:
      - "/spec-sync"
  palette: true
  tui:
    route: "/spec-sync"
states: [empty, loading, success, failure]
validation:
  gates:
    - sv.static.validate
  commands:
    - id: sv.static.validate
      runner: python
      args: ["scripts/sv_static_validate.py"]
      risk: safe_read
      approval: never
docs:
  - docs/workflow-control-plane/VAC_V1_5_ARCHITECTURE.md
