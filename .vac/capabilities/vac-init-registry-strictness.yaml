schema_version: 1
kind: capability
id: vac.init.registry-strictness
title: VAC-Init Registry Strictness and Structured Command Migration
status: ready
owner:
  crate: vac-core
  module: control_plane::vac_init_registry_strictness
ownership:
  crates:
  - vac-core
  modules:
  - control_plane::vac_init_registry_strictness
  targets:
  - kind: module
    crate: vac-core
    module: control_plane::vac_init_registry_strictness
  - kind: path
    include:
    - vac-rs/control-plane/src/control_plane/vac_init_registry_strictness.rs
    - scripts/check-vac-init-registry-strictness-contract.sh
    - .vac/capabilities/vac-init-registry-strictness.yaml
    - .vac/workflows/maintenance.vac-init-registry-strictness.yaml
    - docs/vac-init/VAC_INIT_PRODUCTION_HARDENING_A.md
    - docs/validation/PRODUCTION_HARDENING_A1_A3_VALIDATION.md
    exclude: []
description: Strict-mode registry gate for spec-only manifest kinds, structured validation command objects, duplicate IDs, surface references, planned route visibility, and complete ready capabilities.
depends_on:
- vac.init.registry-validator
- vac.init.command-gate
- vac.init.manifest-contracts
surfaces:
  cli:
    enabled: true
    commands:
    - bash scripts/check-vac-init-registry-strictness-contract.sh
    - vac doctor registry .
  palette: true
  tui:
    routes:
    - /capabilities
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
states:
- empty
- loading
- success
- failure
validation:
  gates:
  - vac_init_registry_strictness_contract
  - structured_validation_commands
  - ready_capability_completeness
  commands:
  - id: vac.init.registry-strictness.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-registry-strictness-contract.sh
    risk: execute_process
    approval: policy
docs:
- docs/vac-init/VAC_INIT_PRODUCTION_HARDENING_A.md
- docs/validation/PRODUCTION_HARDENING_A1_A3_VALIDATION.md
