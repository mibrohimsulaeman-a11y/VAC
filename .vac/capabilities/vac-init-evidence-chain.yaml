schema_version: 1
kind: capability
id: vac.init.evidence-chain
title: VAC-Init Evidence Canonical Hash Chain
status: ready
owner:
  crate: vac-core
  module: control_plane::vac_init_evidence_chain
ownership:
  crates:
  - vac-core
  modules:
  - control_plane::vac_init_evidence_chain
  targets:
  - crate_name: vac-core
    module: control_plane::vac_init_evidence_chain
  - crate_name: vac-surface-cli
    module: doctor_cli
description: Evidence contract provides canonical payload hashing, SHA-256 implementation, self-hash verification, and previous-link chain validation.
depends_on:
- vac.init.patch-guard
- vac.init.approval-binding
surfaces:
  cli:
    enabled: true
    commands:
    - bash scripts/check-vac-init-evidence-chain-contract.sh
    - vac doctor evidence
    - vac doctor evidence --v2
    - vac doctor evidence-signing
  slash:
    commands:
    - /evidence
  palette: true
  tui:
    routes:
    - /evidence
    - /init/evidence-chain
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for:
  - execute_process
states:
- empty
- loading
- success
- failure
validation:
  gates:
  - vac_init_evidence_chain_contract
  commands:
  - id: vac.init.evidence-chain.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-evidence-chain-contract.sh
    risk: execute_process
    approval: policy
