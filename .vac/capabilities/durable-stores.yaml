schema_version: 1
kind: capability
id: vac.init.durable-stores
title: VAC-Init Durable Stores
status: ready
owner:
  crate: vac-core
  module: control_plane::vac_init_durable_stores
ownership:
  crates:
  - vac-core
  modules:
  - control_plane::vac_init_durable_stores
  targets:
  - kind: module
    crate: vac-core
    module: control_plane::vac_init_durable_stores
  - kind: path
    include:
    - vac-rs/crates/control-plane/control-plane/src/control_plane/vac_init_durable_stores.rs
    - scripts/check-vac-init-durable-stores-contract.sh
    - .vac/capabilities/durable-stores.yaml
    - .vac/workflows/maintenance.durable-stores.yaml
    exclude: []
description: Production hardening contract for VAC-Init Durable Stores.
depends_on:
- vac.init.runtime-gate-enforcement
- vac.init.approval-binding
- vac.init.memory-governance
surfaces:
  cli:
    enabled: true
    commands:
    - bash scripts/check-vac-init-durable-stores-contract.sh
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
states:
- empty
- loading
- success
- failure
validation:
  gates:
  - durable_stores
  commands:
  - id: vac.init.durable-stores.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-durable-stores-contract.sh
    risk: execute_process
    approval: policy
