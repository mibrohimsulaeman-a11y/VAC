schema_version: 1
kind: capability
id: vac.sessions
title: Sessions
status: ready
ownership:
  annotation: vac.sessions
  paths:
    include:
      - "vac-rs/crates/capabilities/sessions/**"
      - "vac-rs/crates/capabilities/external-agent-sessions/**"
    exclude:
      - "**/target/**"
policy:
  risk: low
  mutates_files: true
  network: false
  approval_required_for: [session_write, checkpoint_write]
depends_on: [vac.runtime]
