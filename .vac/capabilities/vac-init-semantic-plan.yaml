schema_version: 1
kind: capability
id: vac.init.semantic-plan
title: VAC-Init Semantic Plan Validator
status: ready
owner:
  crate: vac-core
  module: control_plane::vac_init_semantic_plan
ownership:
  crates:
  - vac-core
  modules:
  - control_plane::vac_init_semantic_plan
  targets:
  - crate_name: vac-core
    module: control_plane::vac_init_semantic_plan
description: Pre-plan validator enforces executable capability, ownership, allowed files, bounds, and structured validation commands.
depends_on:
- vac.init.command-gate
- vac.init.ownership-scanner
surfaces:
  cli:
    enabled: true
    commands:
    - bash scripts/check-vac-init-semantic-plan-contract.sh
  palette: true
  tui:
    routes:
    - /init/semantic-plan
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for:
  - execute_process
states:
- empty
- loading
- success
- failure
validation:
  gates:
  - vac_init_semantic_plan_contract
  commands:
  - id: vac.init.semantic-plan.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-semantic-plan-contract.sh
    risk: execute_process
    approval: policy
