schema_version: 2
kind: capability
id: vac.tui
title: TUI
status: ready
ownership:
  annotation: vac.tui
  paths:
    include:
      - "vac-rs/crates/surfaces/tui/**"
    exclude:
      - "**/chatwidget/**"   # chatwidget is owned by vac.chat
      - "**/target/**"
notes:
  - Legacy app-server modules are retired from the runtime path. Tracked as finding F-0002 (waived W-0001) in ledger/, NOT enumerated as retired targets here.
policy:
  risk: low
  mutates_files: false
  network: false
depends_on: [vac.chat, vac.workflow]
