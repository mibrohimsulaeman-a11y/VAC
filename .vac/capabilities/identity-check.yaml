schema_version: 1
kind: capability
id: vac.identity.check
title: Identity Check
status: ready
owner:
  crate: vac-control-plane
  module: control_plane.identity_check
ownership:
  crates:
  - vac-control-plane
  modules:
  - control_plane.identity_check
  targets:
  - crate_name: vac-control-plane
    module: control_plane.identity_check
description: Product identity and legacy term scanning for the root product.
reason: 'Identity-check workflow validation, palette exposure, ownership metadata, and root catalog source-domain mapping are synchronized after Plan 19/21 closeout.

  '
depends_on:
- vac.workflow
surfaces:
  palette: true
  tui:
    routes:
    - /capabilities/vac.identity-check
states:
- empty
- loading
- success
- failure
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
validation:
  commands:
  - id: vac.identity.check.validation.cmd001
    runner: cargo
    args:
    - +1.95.0
    - check
    - -p
    - vac-surface-cli
    risk: execute_process
    approval: policy
