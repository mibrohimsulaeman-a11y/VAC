schema_version: 1
kind: capability
id: vac.init.patch-guard
title: VAC-Init Bounded Patch Guard
status: ready
owner:
  crate: vac-core
  module: control_plane::vac_init_patch_guard
ownership:
  crates:
  - vac-core
  modules:
  - control_plane::vac_init_patch_guard
  targets:
  - crate_name: vac-core
    module: control_plane::vac_init_patch_guard
description: Patch guard enforces plan.allowed_files, operation, line range or anchor, ownership, new-file declarations, forbidden globs, and patch budget.
depends_on:
- vac.init.semantic-plan
- vac.init.approval-binding
surfaces:
  cli:
    enabled: true
    commands:
    - bash scripts/check-vac-init-patch-guard-contract.sh
  palette: true
  tui:
    routes:
    - /capabilities
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for:
  - execute_process
states:
- empty
- loading
- success
- failure
validation:
  gates:
  - vac_init_patch_guard_contract
  commands:
  - id: vac.init.patch-guard.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-patch-guard-contract.sh
    risk: execute_process
    approval: policy
docs:
- docs/vac-init/VAC_INIT_BATCH_06_12_IMPLEMENTATION.md
- docs/validation/VAC_INIT_PATCH_GUARD_GATE.md
