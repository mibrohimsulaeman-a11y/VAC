schema_version: 1
kind: capability
id: vac.init.registry-validator
title: VAC-Init Registry Validator and Doctor Foundation
status: ready
owner:
  crate: vac-core
  module: control_plane::vac_init_registry_validator
ownership:
  crates:
  - vac-core
  modules:
  - control_plane::vac_init_registry_validator
  targets:
  - crate_name: vac-core
    module: control_plane::vac_init_registry_validator
description: Dependency-free registry validator foundation for envelope checks, ID uniqueness, kind compatibility diagnostics, ready capability required fields, and doctor-ready diagnostic rendering.
depends_on:
- vac.init.schema-envelope
- vac.init.manifest-contracts
surfaces:
  cli:
    enabled: true
    commands:
    - bash scripts/check-vac-init-registry-validator-contract.sh
    - vac doctor registry .
  palette: true
  tui:
    routes:
    - /capabilities
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
states:
- empty
- loading
- success
- failure
validation:
  commands:
  - id: vac.init.registry-validator.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-registry-validator-contract.sh
    risk: execute_process
    approval: policy
  gates:
  - vac_init_registry_validator_contract
docs:
- docs/vac-init/VAC_INIT_BATCH_02_05_IMPLEMENTATION.md
- docs/validation/VAC_INIT_REGISTRY_VALIDATOR_GATE.md
