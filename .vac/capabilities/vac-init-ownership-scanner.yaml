schema_version: 1
kind: capability
id: vac.init.ownership-scanner
title: VAC-Init Source Inventory and Ownership Scanner
status: ready
owner:
  crate: vac-core
  module: control_plane::vac_init_ownership_scanner
ownership:
  crates:
  - vac-core
  modules:
  - control_plane::vac_init_ownership_scanner
  targets:
  - crate_name: vac-core
    module: control_plane::vac_init_ownership_scanner
description: VAC-Init source inventory and ownership scan foundation for file classification, ownership target matching, unowned/overclaimed detection, and quarantine action suggestions.
depends_on:
- vac.init.manifest-contracts
- vac.init.registry-validator
surfaces:
  cli:
    enabled: true
    commands:
    - bash scripts/check-vac-init-ownership-scanner-contract.sh
  palette: true
  tui:
    routes:
    - /init/ownership
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
states:
- empty
- loading
- success
- failure
validation:
  commands:
  - id: vac.init.ownership-scanner.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-ownership-scanner-contract.sh
    risk: execute_process
    approval: policy
  gates:
  - vac_init_ownership_scanner_contract
