schema_version: 1
kind: capability
id: vac.init.command-gate
title: VAC-Init Structured Command Gate
status: ready
owner:
  crate: vac-core
  module: control_plane::vac_init_command_gate
ownership:
  crates:
  - vac-core
  modules:
  - control_plane::vac_init_command_gate
  targets:
  - crate_name: vac-core
    module: control_plane::vac_init_command_gate
description: Structured command evaluator rejects free-form shell, runner paths, and shell metacharacters while policy-gating execute_process.
depends_on:
- vac.init.policy-evaluator
surfaces:
  cli:
    enabled: true
    commands:
    - bash scripts/check-vac-init-command-gate-contract.sh
  palette: true
  tui:
    routes:
    - /capabilities
policy:
  risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for:
  - execute_process
states:
- empty
- loading
- success
- failure
validation:
  gates:
  - vac_init_command_gate_contract
  commands:
  - id: vac.init.command-gate.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-command-gate-contract.sh
    risk: execute_process
    approval: policy
docs:
- docs/vac-init/VAC_INIT_BATCH_06_12_IMPLEMENTATION.md
- docs/validation/VAC_INIT_COMMAND_GATE.md
