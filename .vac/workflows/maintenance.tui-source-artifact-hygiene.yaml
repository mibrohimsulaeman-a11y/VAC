schema_version: 1
kind: workflow
id: maintenance.tui-source-artifact-hygiene
title: TUI Source Artifact Hygiene Gate
status: ready
owner:
  crate: vac-surface-tui
  module: operator_ui
triggers:
- kind: manual
  id: check_tui_source_artifact_hygiene
steps:
- id: source_artifact_hygiene
  uses: capability.vac.tui.source-artifact-hygiene.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
policy:
  mutates_files: false
  network: false
  default_risk: safe_read
  redaction: false
  approval_required_for: []
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
validation:
  gates:
  - source_artifact_hygiene
  commands:
  - id: maintenance.tui-source-artifact-hygiene.validation.cmd001
    runner: bash
    args:
    - scripts/check-tui-source-artifact-hygiene.sh
    risk: execute_process
    approval: policy
