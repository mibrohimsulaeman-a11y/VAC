schema_version: 1
kind: workflow
id: maintenance.vac-init-doctor-release
title: VAC-Init Doctor Release Contract Check
status: ready
inputs: {}
steps:
- id: vac_init_doctor_release_contract
  uses: capability.vac.init.doctor-release.check
  depends_on: []
  ui:
    surface: /capabilities
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for:
  - execute_process
validation:
  gates:
  - vac_init_doctor_release_contract
  commands:
  - id: vac.init.doctor-release.validation.contract
    runner: bash
    args:
    - scripts/check-vac-init-doctor-release-contract.sh
    risk: execute_process
    approval: policy
ui:
  surface: /capabilities
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
