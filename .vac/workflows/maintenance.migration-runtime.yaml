schema_version: 1
kind: workflow
id: maintenance.migration-runtime
title: VAC-Init Migration Runtime and Compatibility Cleanup readiness
status: ready
inputs: {}
steps:
- id: migration_protocol_runtime
  uses: capability.vac.init.migration-runtime.check
  ui:
    surface: /runtime
    progress_panel: true
    activity_log: true
    approval_surface: true
    evidence_surface: true
- id: compatibility_kind_cleanup
  uses: capability.vac.init.migration-runtime.check
  depends_on:
  - migration_protocol_runtime
  ui:
    surface: /runtime
    progress_panel: true
    activity_log: true
    approval_surface: true
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /runtime
  progress_panel: true
  activity_log: true
  approval_surface: true
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - compatibility_kind_cleanup
  - migration_protocol_runtime
  - migration_runtime
  commands:
  - id: vac.init.migration-runtime.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-migration-runtime-contract.sh
    risk: execute_process
    approval: policy
