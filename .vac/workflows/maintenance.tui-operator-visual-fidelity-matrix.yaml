schema_version: 1
kind: workflow
id: maintenance.tui-operator-visual-fidelity-matrix
title: TUI Operator Visual Fidelity Matrix Gate
status: ready
owner:
  crate: vac-surface-tui
  module: operator_ui
triggers:
- kind: manual
  id: check_tui_operator_visual_fidelity_matrix
steps:
- id: render_plain_and_ansi_matrix
  uses: capability.vac.tui.operator-visual-fidelity-matrix.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
policy:
  mutates_files: false
  network: false
  default_risk: safe_read
  redaction: false
  approval_required_for: []
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
validation:
  gates:
  - render_plain_and_ansi_matrix
  commands:
  - id: maintenance.tui-operator-visual-fidelity-matrix.validation.cmd001
    runner: bash
    args:
    - scripts/check-tui-operator-visual-fidelity-matrix.sh
    risk: execute_process
    approval: policy
