schema_version: 1
kind: workflow
id: maintenance.vac-init-registry-strictness
title: VAC-Init Production Hardening A registry strictness gate
status: ready
inputs: {}
steps:
- id: strict_kind_audit
  uses: capability.registry.schema_check
  ui:
    surface: /capabilities
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
- id: structured_command_audit
  uses: capability.vac.init.registry-strictness.check
  depends_on:
  - strict_kind_audit
  ui:
    surface: /capabilities
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
- id: ready_capability_completeness
  uses: capability.vac.init.registry-strictness.check
  depends_on:
  - structured_command_audit
  ui:
    surface: /capabilities
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - ready_capability_completeness
  - strict_kind_audit
  - structured_command_audit
  - vac_init_registry_strictness_contract
  commands:
  - id: maintenance.vac-init-registry-strictness.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-registry-strictness-contract.sh
    risk: execute_process
    approval: policy
