schema_version: 1
kind: workflow
id: maintenance.global-release-blockers
title: Global legal/build release blockers
status: ready
inputs: {}
steps:
- id: check
  uses: capability.vac.release.legal-build-blockers.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /release
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - global_release_blockers
  commands:
  - id: maintenance.global-release-blockers.validation.cmd001
    runner: bash
    args:
    - scripts/check-legal-release-blockers.sh
    risk: execute_process
    approval: policy
