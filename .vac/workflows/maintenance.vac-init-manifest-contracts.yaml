schema_version: 1
kind: workflow
id: maintenance.vac-init-manifest-contracts
title: VAC-Init typed manifest contracts readiness
status: ready
inputs: {}
steps:
- id: manifest_contracts
  uses: capability.registry.schema_check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
- id: surface_lifecycle_guard
  uses: capability.vac.init.manifest-contracts.check
  depends_on:
  - manifest_contracts
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - manifest_contracts
  - surface_lifecycle_guard
  - vac_init_manifest_structs_contract
  commands:
  - id: maintenance.vac-init-manifest-contracts.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-manifest-structs-contract.sh
    risk: execute_process
    approval: policy
