schema_version: 1
kind: workflow
id: maintenance.docs-state-refresh
title: VAC documentation state refresh gate
status: ready
inputs: {}
steps:
- id: docs_state_refresh
  uses: capability.vac.docs.state-refresh.check
  ui:
    surface: /docs
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /docs
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - docs_state_refresh
  commands:
  - id: maintenance.docs-state-refresh.validation.cmd001
    runner: bash
    args:
    - scripts/check-docs-state-refresh.sh
    risk: execute_process
    approval: policy
