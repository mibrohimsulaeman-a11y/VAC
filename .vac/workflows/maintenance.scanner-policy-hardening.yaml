schema_version: 1
kind: workflow
id: maintenance.scanner-policy-hardening
title: VAC-Init Scanner and Policy Inference Hardening readiness
status: ready
inputs: {}
steps:
- id: ownership_scanner_upgrade
  uses: capability.vac.init.scanner-policy-hardening.check
  ui:
    surface: /runtime
    progress_panel: true
    activity_log: true
    approval_surface: true
    evidence_surface: true
- id: rust_risk_scanner_ast
  uses: capability.vac.init.scanner-policy-hardening.check
  depends_on:
  - ownership_scanner_upgrade
  ui:
    surface: /runtime
    progress_panel: true
    activity_log: true
    approval_surface: true
    evidence_surface: true
- id: policy_inference_from_risk
  uses: capability.vac.init.scanner-policy-hardening.check
  depends_on:
  - rust_risk_scanner_ast
  ui:
    surface: /runtime
    progress_panel: true
    activity_log: true
    approval_surface: true
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /runtime
  progress_panel: true
  activity_log: true
  approval_surface: true
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - ownership_scanner_upgrade
  - policy_inference_from_risk
  - rust_risk_scanner_ast
  - scanner_policy_hardening
  commands:
  - id: vac.init.scanner-policy-hardening.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-scanner-policy-hardening-contract.sh
    risk: execute_process
    approval: policy
