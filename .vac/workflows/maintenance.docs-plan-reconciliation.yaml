schema_version: 1
kind: workflow
id: maintenance.docs-plan-reconciliation
title: Docs plan codebase reconciliation maintenance
status: ready
inputs: {}
steps:
- id: reconcile_plan_status
  uses: capability.vac.docs.plan-reconciliation.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /docs
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - docs_plan_codebase_reconciliation
  commands:
  - id: maintenance.docs-plan-reconciliation.validation.cmd001
    runner: bash
    args:
    - scripts/check-plan-codebase-reconciliation.sh
    risk: execute_process
    approval: policy
