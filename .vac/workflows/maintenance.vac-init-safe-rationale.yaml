schema_version: 1
kind: workflow
id: maintenance.vac-init-safe-rationale
title: VAC-Init Safe Rationale Contract Check
status: ready
inputs: {}
steps:
- id: vac_init_safe_rationale_contract
  uses: capability.vac.init.safe-rationale.check
  depends_on: []
  ui:
    surface: /capabilities
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for:
  - execute_process
validation:
  gates:
  - vac_init_safe_rationale_contract
  commands:
  - id: vac.init.safe-rationale.validation.contract
    runner: bash
    args:
    - scripts/check-vac-init-why-contract.sh
    risk: execute_process
    approval: policy
ui:
  surface: /capabilities
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
docs:
- docs/validation/VAC_INIT_SAFE_RATIONALE_GATE.md
