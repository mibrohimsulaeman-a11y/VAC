schema_version: 1
kind: workflow
id: maintenance.ownership-scan
title: Ownership scan
status: ready
inputs: {}
steps:
- id: orient
  uses: capability.ownership.scan
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
- id: report
  uses: capability.activity.emit
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  commands:
  - id: maintenance.ownership-scan.validation.cmd001
    runner: cargo
    args:
    - +1.93.0
    - run
    - -p
    - vac-cli
    - --
    - doctor
    - ownership
    - .
    risk: execute_process
    approval: policy
  gates:
  - orient
  - report
