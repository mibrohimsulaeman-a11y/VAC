schema_version: 1
kind: workflow
id: maintenance.vac-init-memory-governance
title: VAC-Init Memory Governance Contract Check
status: ready
inputs: {}
steps:
- id: vac_init_memory_governance_contract
  uses: capability.vac.init.memory-governance.check
  depends_on: []
  ui:
    surface: /capabilities
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for:
  - execute_process
validation:
  gates:
  - vac_init_memory_governance_contract
  commands:
  - id: vac.init.memory-governance.validation.contract
    runner: bash
    args:
    - scripts/check-vac-init-memory-governance-contract.sh
    risk: execute_process
    approval: policy
ui:
  surface: /capabilities
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
docs:
- docs/validation/VAC_INIT_MEMORY_GOVERNANCE_GATE.md
