schema_version: 1
kind: workflow
id: maintenance.tui-spec-driven-consolidation
title: TUI Spec-Driven Consolidation Gate
status: ready
owner:
  crate: vac-surface-tui
  module: operator_ui
triggers:
- kind: manual
  id: check_tui_spec_driven_consolidation
steps:
- id: spec_driven_consolidation
  uses: capability.vac.tui.spec-driven-consolidation.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
policy:
  mutates_files: false
  network: false
  default_risk: safe_read
  redaction: false
  approval_required_for: []
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
validation:
  gates:
  - spec_driven_consolidation
  commands:
  - id: maintenance.tui-spec-driven-consolidation.validation.cmd001
    runner: bash
    args:
    - scripts/check-tui-spec-driven-consolidation.sh
    risk: execute_process
    approval: policy
