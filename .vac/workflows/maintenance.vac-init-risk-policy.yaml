schema_version: 1
kind: workflow
id: maintenance.vac-init-risk-policy
title: VAC-Init Risk Scanner and Policy Inference readiness
status: ready
inputs: {}
steps:
- id: contract
  uses: capability.vac.init.risk-policy.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - contract
  - vac_init_risk_policy_contract
  commands:
  - id: maintenance.vac-init-risk-policy.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-risk-policy-contract.sh
    risk: execute_process
    approval: policy
