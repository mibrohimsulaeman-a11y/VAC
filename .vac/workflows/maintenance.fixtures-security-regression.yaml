schema_version: 1
kind: workflow
id: maintenance.fixtures-security-regression
title: VAC-Init Fixture Matrix and Security Regression readiness
status: ready
inputs: {}
steps:
- id: fixture_matrix_complete
  uses: capability.vac.init.fixtures-security-regression.check
  ui:
    surface: /runtime
    progress_panel: true
    activity_log: true
    approval_surface: true
    evidence_surface: true
- id: cargo_gate_plan_registered
  uses: capability.vac.init.fixtures-security-regression.check
  depends_on:
  - fixture_matrix_complete
  ui:
    surface: /runtime
    progress_panel: true
    activity_log: true
    approval_surface: true
    evidence_surface: true
- id: security_regression_attacks_rejected
  uses: capability.vac.init.fixtures-security-regression.check
  depends_on:
  - cargo_gate_plan_registered
  ui:
    surface: /runtime
    progress_panel: true
    activity_log: true
    approval_surface: true
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /runtime
  progress_panel: true
  activity_log: true
  approval_surface: true
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - cargo_gate_plan_registered
  - fixture_matrix_complete
  - fixtures_security_regression
  - security_regression_attacks_rejected
  commands:
  - id: vac.init.fixtures-security-regression.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-fixtures-security-regression-contract.sh
    risk: execute_process
    approval: policy
