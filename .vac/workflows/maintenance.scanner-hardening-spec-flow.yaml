schema_version: 1
kind: workflow
id: maintenance.scanner-hardening-spec-flow
title: VAC-Init scanner hardening spec-flow
status: ready
inputs: {}
steps:
- id: scanner_wiring
  uses: capability.vac.init.scanner-hardening-spec-flow.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
- id: risk_storage
  uses: capability.vac.init.scanner-hardening-spec-flow.check
  depends_on:
  - scanner_wiring
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
- id: policy_inference
  uses: capability.vac.init.scanner-hardening-spec-flow.check
  depends_on:
  - risk_storage
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
- id: evidence
  uses: capability.vac.init.scanner-hardening-spec-flow.check
  depends_on:
  - policy_inference
  ui:
    surface: /evidence
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: true
  network: false
  redaction: true
  approval_required_for:
  - filesystem_write
validation:
  gates:
  - scanner_hardening_spec_flow
  commands:
  - id: maintenance.scanner-hardening-spec-flow.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-scanner-hardening-spec-flow.sh
    risk: execute_process
    approval: policy
