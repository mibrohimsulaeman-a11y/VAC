schema_version: 1
kind: workflow
id: maintenance.vac-init-registry-validator
title: VAC-Init registry validator readiness
status: ready
inputs: {}
steps:
- id: registry_validator_contract
  uses: capability.registry.schema_check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
- id: id_uniqueness
  uses: capability.vac.init.registry-validator.check
  depends_on:
  - registry_validator_contract
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - id_uniqueness
  - registry_validator_contract
  - vac_init_registry_validator_contract
  commands:
  - id: maintenance.vac-init-registry-validator.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-registry-validator-contract.sh
    risk: execute_process
    approval: policy
