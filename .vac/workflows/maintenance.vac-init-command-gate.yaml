schema_version: 1
kind: workflow
id: maintenance.vac-init-command-gate
title: VAC-Init Structured Command Gate readiness
status: ready
inputs: {}
steps:
- id: contract
  uses: capability.vac.init.command-gate.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - contract
  - vac_init_command_gate_contract
  commands:
  - id: maintenance.vac-init-command-gate.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-command-gate-contract.sh
    risk: execute_process
    approval: policy
