schema_version: 1
kind: workflow
id: maintenance.vac-init-policy-evaluator
title: VAC-Init Fail-Closed Policy Evaluator readiness
status: ready
inputs: {}
steps:
- id: contract
  uses: capability.vac.init.policy-evaluator.check
  ui:
    surface: /workflow
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /workflow
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  gates:
  - contract
  - vac_init_policy_evaluator_contract
  commands:
  - id: maintenance.vac-init-policy-evaluator.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-policy-evaluator-contract.sh
    risk: execute_process
    approval: policy
