schema_version: 1
kind: workflow
id: maintenance.vac-init-baseline-audit
title: VAC-Init Baseline Audit
status: ready
inputs: {}
steps:
- id: baseline_audit
  uses: capability.registry.schema_check
  ui:
    surface: /capabilities
    progress_panel: true
    activity_log: true
    approval_surface: false
    evidence_surface: true
ui:
  surface: /capabilities
  inspect_surface: /capabilities
  progress_panel: true
  activity_log: true
  approval_surface: false
  evidence_surface: true
policy:
  default_risk: safe_read
  mutates_files: false
  network: false
  redaction: false
  approval_required_for: []
validation:
  commands:
  - id: maintenance.vac-init-baseline-audit.validation.cmd001
    runner: bash
    args:
    - scripts/check-vac-init-baseline-contract.sh
    risk: execute_process
    approval: policy
  gates:
  - baseline_audit
